export const RoomsTable = () => {
  // TODO: get all of our rooms
  // const { loading, error, data } = useQuery(GET_APPROVED_ORGANIZATIONS)

  return (
    <>
      <div>rooms listed here</div>
    </>
  );
};
